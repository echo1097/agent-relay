{
  "name": "agent-relay-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Relay desktop scaffold",
  "main": "electron/main.cjs",
  "scripts": {
    "build": "vite build",
    "start": "electron .",
    "test": "node --test electron/*.test.cjs"
  },
  "devDependencies": {
    "electron": "^41.0.0",
    "vite": "^7.0.0"
  }
}
